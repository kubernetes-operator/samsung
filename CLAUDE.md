## 하네스: Kubernetes 트래픽 기반 자동운영 오퍼레이터

**목표:** Pod의 CPU/메모리가 아닌 서비스 트래픽(RPS/에러율/지연시간, Gateway API 기반)을 기준으로 스케일링·장애감지·라우팅제어·이상탐지를 자동 수행하는 Python kopf 오퍼레이터를 5인 에이전트 팀으로 구축·유지보수한다.

**트리거:** 이 오퍼레이터 관련 작업 요청 시 `k8s-traffic-ops-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-19 | 초기 구성 (5인 팀: operator-architect, metrics-collector-dev, policy-engine-dev, actuator-dev, qa-engineer + 오케스트레이터) | 전체 | 신규 프로젝트, Python+kopf / Gateway API 트래픽 지표 기반으로 결정 |
| 2026-07-19 | Ralph Loop 연동 추가 — QA 전체 PASS 시 `<promise>` 출력, Phase 0에 "QA 재작업 재개" 분기 추가 | skills/k8s-traffic-ops-orchestrator | ralph-loop 플러그인으로 QA PASS까지 무인 반복 실행을 지원하기 위함 |
| 2026-07-19 | 오퍼레이터 최초 빌드 완료 (`operator/` 전체, ~2,100줄) — QA 5개 경계면 전부 PASS | operator/ | TeamCreate 미지원 환경이라 서브 에이전트 모드로 실행(architect 순차 → metrics/policy/actuator 병렬 → QA). FINDING-1: policy가 `reroute` action을 생성하지 않아 CRD/스키마의 reroute(카나리) 기능이 미구현 상태 — 후속 결정 필요 |
| 2026-07-19 | FINDING-1 해결 — policy/engine.py에 isolate_backend 복구용 reroute 로직 추가, 구현 중 발견된 복구 조기중단/cooldown 상호작용 버그 2건 동시 수정 | operator/k8s_traffic_operator/policy/engine.py | 사용자가 "지금 policy에 구현" 선택. reroute 없이는 격리된 backend가 영구히 낮은 weight에 머무는 결함이었음 |
| 2026-07-19 | nginx Gateway Fabric 어댑터 추가 (코드 구현 + mock 검증) | operator/.../metrics/adapters/nginx_gateway_fabric.py | 실제 검증 시도 중 이 프로젝트가 연결된 클러스터의 실제 Gateway API 구현체가 nginx Gateway Fabric임을 확인(기존엔 Envoy Gateway/Istio만 지원). 단, 해당 클러스터엔 stub_status/메트릭 노출이 없어 실제 E2E는 미검증 — 사용자 결정으로 클러스터에 리소스는 생성하지 않음. NGINX Plus API는 latency percentile 미제공이라 p95/p99는 의도적으로 None |
| 2026-07-19 | pytest 정식 회귀 테스트 스위트 추가 (10개 파일, 116개 테스트) | operator/tests/, operator/pytest.ini, operator/.venv | 지금까지 인라인 스크립트로 임시 검증했던 것(reroute 복구 버그 2건 포함)을 정식 회귀 테스트로 고정. kubernetes client는 MagicMock, Prometheus는 PrometheusClient의 session 주입 지점으로 mock. 실제 클러스터/kopf 이벤트 루프 E2E는 여전히 범위 밖 |
| 2026-07-19 | 실제 클러스터 E2E 검증 수행 후 정리 — 전용 네임스페이스(traffic-ops-test)에 CRD/더미 backend/HTTPRoute/최소권한 RBAC/CR을 실배포, SA 자격증명으로 kopf 실행하여 collection_failed/no_data 안전 경로 확인, 합성 Decision으로 actuator의 실제 scale/reroute 쓰기 경로(변경폭 clamp·idempotent 포함) 검증. 검증 후 전부 삭제 | (클러스터, 저장소 변경 없음) | 사용자 요청으로 실제 배포 검증. 클러스터는 nginx Gateway Fabric이 트래픽 메트릭을 노출하지 않아 정책 로직 자체는 발동되지 않았지만, kopf reconcile 배선과 actuator 쓰기 경로는 실제 API 서버 대상으로 확인됨 |
| 2026-07-19 | 읽기 전용 웹 대시보드 추가 (`dashboard/`) | operator/k8s_traffic_operator/dashboard/, operator/deploy/dashboard.yaml, operator/Dockerfile.dashboard | 사용자가 오퍼레이터 운영 현황을 웹페이지로 보고 싶어함. FastAPI 기반, 클러스터엔 get/list/watch만(쓰기 없음), 오퍼레이터 본체와 별도 프로세스로 배포. 12개 테스트 추가(총 128개) |
| 2026-07-19 | 대시보드를 실제 클러스터에 배포 (`traffic-policy-dashboard` 네임스페이스, 내부 레지스트리 registry.local.cloud:5000 사용) | 클러스터 | 사용자 요청. `https://test2.studiobasa.com/traffic-dashboard/`로 실제 브라우저 접속 확인(기존 앱들과 같은 호스트명 관례 사용, 전용 서브도메인은 DNS에 없어 실패했었음 — HTTPRoute에 URLRewrite 필터 추가로 prefix 제거 후 정상 동작). 인증 없이 공개 노출된 상태이니 운영 시 접근 제어 추가 필요 |
| 2026-07-19 | CRD/실제 TrafficPolicy CR 재배포 후 테스트 — kopf 재실행으로 실제 reconcile 확인, 대시보드에 실제 반영됨을 확인 | 클러스터 (전과 동일 traffic-ops-test) | 사용자 요청. 대시보드에 traffic-ops-test(합성 테스트 앱)만 보이는 것에 대해 사용자가 문제 제기 → 이 클러스터는 Prometheus가 애플리케이션 네임스페이스를 아예 스크랩하지 않는다는 추가 한계도 발견됨(Gateway 메트릭 미노출과 별개) |
| 2026-07-19 | Hubble(Cilium CNI) 기반 실시간 Pod 트래픽 흐름 대시보드 추가 (`/flows`) | operator/k8s_traffic_operator/dashboard/hubble_flows.py, app.py, Dockerfile.dashboard, deploy/dashboard.yaml | 사용자가 "Gateway API와 CNI를 통해서 Pod 트래픽 상황을 모니터링"을 요청. 이 클러스터에 Cilium/Hubble이 이미 떠 있어(enable-hubble=true) 별도 인프라 변경 없이 실제 L3/L4 Pod 트래픽 흐름 관측 가능함을 실측 확인. hubble CLI를 서브프로세스로 호출(버전이 클러스터 Cilium과 안 맞으면 "invalid fieldmask"로 실패 — Dockerfile의 HUBBLE_CLI_VERSION으로 고정). 사용자 결정으로 범위는 시각화만(정책 엔진의 스케일링 판단에는 아직 연결 안 함). 22개 테스트 추가(총 150개) |
| 2026-07-19 | Hubble을 정책 엔진 스케일링 판단에도 연결 (`GATEWAY_IMPLEMENTATION=cilium-hubble`) | operator/k8s_traffic_operator/hubble_client.py(신규, dashboard와 공유), metrics/hubble_collector.py(신규), metrics/collector.py | 사용자 요청으로 시각화 전용에서 확장. rps=연결수/윈도우초(HTTP 요청 아님), error_rate=비FORWARDED 비율(HTTP 5xx 아님)로 명확히 구분 문서화. latency/per_backend는 의도적으로 미제공(isolate_backend/reroute는 이 소스로 트리거 안 됨 — backendRef 매핑 불확실성으로 인한 안전 설계). traffic-ops-test에 실제 트래픽 발생시켜 collect()→policy.evaluate() 전체 파이프라인 실제 클러스터 검증 완료. 18개 테스트 추가(총 168개) |
| 2026-07-19 | 오퍼레이터 본체를 클러스터에 상시 배포 (`traffic-ops-system` 네임스페이스, `GATEWAY_IMPLEMENTATION=cilium-hubble`) | operator/Dockerfile(신규), operator/deploy/operator.yaml(신규) | 사용자 요청 — 지금까지는 로컬 kopf 프로세스로만 검증했으나 이제 클러스터 상시 워크로드로 전환. `-A`(전체 네임스페이스) 모드라 ClusterRole 필요(쓰기는 각 CR의 target에만 적용). PYTHONPATH 미설정으로 최초 기동 실패 발견·수정(kopf CLI가 cwd를 sys.path에 자동으로 안 넣음). 배포 후 traffic-ops-test에 실제 트래픽을 발생시켜 지속 실행 중인 인스턴스가 실시간으로 감지·판단·대시보드 반영하는 것까지 확인 |
| 2026-07-23 | `/flows` 대시보드에 애플리케이션/인프라 트래픽 구분 추가 후 재배포 | operator/k8s_traffic_operator/dashboard/{app.py,hubble_flows.py}, operator/tests/{test_hubble_flows.py,test_dashboard.py}, operator/Dockerfile.dashboard | 인프라 노이즈(coredns/health check/gpu-operator/velero 등)에 사용자 앱 Pod 트래픽이 묻히는 문제 해결. `is_application_flow()`로 분류(reserved 개체·시스템 ns면 인프라, 한쪽이라도 앱 ns면 애플리케이션 흐름), 대시보드 기본 `scope=app`·`scope=all` 토글. **Dockerfile.dashboard 버그 발견·수정**: hubble_client 분리(02f72c4) 이후로도 Dockerfile이 `hubble_client.py`를 COPY하지 않아, 재빌드 시 기동 즉시 ImportError가 날 상태였음(현재 실행 중이던 파드는 분리 이전 구버전 이미지였기에 우연히 살아있었음). 재빌드·푸시·rollout 후 실측: 전체 2,392건 흐름 중 앱 흐름 198건만 기본 노출(인프라 2,194건 필터), Hubble 조회 정상. 176개 테스트 전부 PASS |
| 2026-07-23 | 대시보드에 HTTP Basic 인증 추가(공개 노출 접근 제어) | operator/k8s_traffic_operator/dashboard/app.py, operator/deploy/dashboard.yaml, operator/tests/test_dashboard.py | 그간 무인증 공개 상태였던 대시보드를 앱 레벨 Basic 인증으로 보호. `/healthz`만 예외(k8s 프로브). 자격증명은 코드/이미지/저장소 어디에도 없고 env(DASHBOARD_USERNAME/PASSWORD)로만 주입 — 배포는 `traffic-policy-dashboard-auth` Secret→env(Secret은 커밋 안 함, 조직 정책상 값은 사용자가 직접 `kubectl create secret`으로 생성). env 미설정 시 전 경로 503 fail-closed로 실수 공개 방지. 사용자명/비밀번호 모두 `secrets.compare_digest` 상수시간 비교. 인증 테스트 7개 추가(총 183개 PASS). 재빌드·푸시·rollout 후 라이브 검증: 인증 없음→401+WWW-Authenticate 챌린지, /api도 401, 오자격증명 401, /healthz만 200 |
| 2026-07-23 | 서브패스(`/traffic-dashboard`) 노출 시 `/flows` 내비게이션 404 버그 수정 | operator/k8s_traffic_operator/dashboard/app.py, operator/deploy/dashboard.yaml, operator/tests/test_dashboard.py | 사용자가 "로그인은 되는데 /flows가 안 된다" 제보. 원인: HTTPRoute가 프리픽스를 떼어 백엔드로 전달하는데 HTML 링크가 절대경로(`/flows`,`/`)라, 브라우저가 `/traffic-dashboard/`에서 링크를 누르면 프리픽스 밖 `/flows`로 나가 **같은 호스트의 다른 앱**(200, 엉뚱한 내용)에 도달 → "구현 안 된 것처럼" 보였음(루트는 URL 직접 입력이라 정상). 수정: 라우트는 그대로 두고 HTML 링크에만 `DASHBOARD_URL_PREFIX`(env) 프리픽스를 붙임(nav·스코프 토글). deploy에 `DASHBOARD_URL_PREFIX=/traffic-dashboard` 추가. **주의: 이미지의 :latest 태그 때문에 코드 변경은 rollout restart로 반영되지만, env/매니페스트 변경은 반드시 `kubectl apply`를 먼저 해야 함**(이번에 restart만 하고 apply를 놓쳐 env 미반영을 한 번 겪음). 라이브 검증: `/traffic-dashboard/flows`→백엔드 401(라우트 도달 확인), env 적용 확인. 프리픽스 링크/정규화 테스트 2개 추가(총 185 PASS) |
| 2026-07-23 | `/flows` 페이지에 설명 패널 + 흐름 다이어그램 추가(`/loop` 동적모드로 진행) | operator/k8s_traffic_operator/dashboard/{app.py,hubble_flows.py}, operator/tests/{test_dashboard.py,test_hubble_flows.py} | 사용자가 "이 데이터가 무엇을 표현하는지 모르겠다"고 함. (1) 상시 설명 패널: 이 화면이 Cilium/Hubble의 실제 L3/L4 Pod 연결이며 화살표=방향·굵기=연결수·색=verdict, Count는 RPS 아님, `/`(정책)와 다른 데이터임을 안내 + verdict 범례. (2) source→destination 노드-링크 다이어그램: 외부 JS/라이브러리 없이 서버 인라인 SVG(자동 새로고침·오프라인 동작), 선 굵기 ∝ 연결수, 색 = 쌍별 대표 verdict, 화살표 `<title>` hover 툴팁, 가독성 위해 상위 10개만(표엔 전체). hubble_flows.summarize에 쌍별 대표 verdict 추가, 표에 Verdict 컬럼 추가. dataviz 스킬 기준 적용(verdict=상태색+텍스트라벨 병행). 좌표 경계 프로그램 검증. 테스트 5개 추가(총 190 PASS). 재빌드·배포·라우팅 도달 확인 |
| 2026-07-23 | `/flows`에 네임스페이스 필터 + 연결된 리소스 선택(focus) 추가(`/loop` 동적모드) | operator/k8s_traffic_operator/dashboard/{hubble_flows.py,app.py}, operator/tests/{test_hubble_flows.py,test_dashboard.py} | 사용자가 "네임스페이스별로, 그리고 연결된 리소스를 선택해서" 보고 싶어함. summarize/fetch_summary에 `namespace`·`focus` 필터를 scope 뒤 3단계로 순차 적용(namespace=한쪽이라도 낀 ns, focus=그 endpoint 라벨이 한쪽 끝인 흐름만). 필터 UI용 `namespaces` 목록은 scope만 적용한 기준으로 산출(어떤 필터든 자유 전환). 대시보드: 네임스페이스 선택 링크(흐름수 표시·active·'전체'), focus 배너+해제, 표의 Source·Destination과 SVG 다이어그램 노드를 클릭하면 그 리소스 focus(라벨의 `/`는 %2F 인코딩), 스코프 토글은 ns/focus 유지·개별 ns 링크는 focus 초기화. 필터로 0건일 때 안내 문구 분기. `/api/flows`에 namespace/focus/namespaces 추가. **이전 `/loop` 이터레이션이 중단돼 FlowSummary 필드만 추가되고 로직은 비어 있던 상태를 이어서 완성.** 테스트 15개 추가(총 205 PASS). 코드/테스트만 완료 — 클러스터 재배포는 미실행(사용자 확인 대기) |
| 2026-07-24 | `/flows` 다이어그램을 bipartite→멀티홉(계층) 그래프로 전환 + 흐름에 쓰이는 리소스(워크로드) 표기(`/loop` 동적모드) | operator/k8s_traffic_operator/dashboard/{hubble_flows.py,app.py}, operator/tests/{test_hubble_flows.py,test_dashboard.py} | 사용자가 "1:1 매칭은 보이는데 다음 단계로 이어지는 흐름과, 그 흐름에 쓰이는 리소스도 같이 보고 싶다"고 함. 기존 좌(source)/우(destination) 2열 도식은 같은 Pod가 양쪽에 중복돼 사슬이 안 보였음. `_assign_layers()`로 top_pairs를 방향 그래프로 보고 진입점부터의 최장 홉 수를 계층으로 매겨(순환 안전: 노드 수만큼만 완화 + 자기간선 무시 + 계층값 0..k 연속 압축) 왼→오로 펼침 → A→B, B→C가 A→B→C 사슬로 이어져 보임(1:1은 인접 두 열). summarize가 노드별 리소스 메타(namespace/workload/kind)를 수집해 `FlowSummary.nodes`로 노출, `/api/flows`에도 nodes 추가. app.py는 `_flow_diagram_svg`→`_flow_graph_svg`로 교체: 리소스 칩(윗줄 ns/pod·아랫줄 ▸워크로드·테두리색=앱/인프라/예약), 가변폭 열 레이아웃, 칩 클릭 시 focus. 설명 패널/캡션도 멀티홉 안내로 갱신. 라이브 실측: scope=all에서 노드 19개·계층 [0,1,2](멀티홉 관측됨)·19개 중 15개 워크로드 채워짐(예 velero/minio-0→minio) 확인. 테스트 11개 추가(총 216 PASS). 재빌드·푸시·rollout 후 라우팅 도달 확인, 검증 중 128Mi→256Mi 임시 상향(내 검증용 2차 인터프리터 OOM 회피) 후 커밋값 128Mi 원복 |
| 2026-07-24 | `/flows` 멀티홉 그래프의 화살표 겹침 해소(레이아웃 재설계, `/loop` 동적모드) | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py | 사용자가 "화살표가 너무 겹친다, 겹치지 않게, 화면 넓게 써도 된다"고 함. `_flow_graph_svg` 레이아웃 재설계: (1) **barycenter 휴리스틱**으로 각 열 노드 순서를 이웃 열 평균 위치에 맞춰 재정렬(위/아래 스윕 4회)해 선 교차 감소, (2) 한 노드에 여러 간선이 붙을 때 **연결 포트를 칩 높이에 분산**(상대편 y로 정렬 후 균등 배치)해 화살표가 한 점에 몰리지 않게, (3) 짧은 열은 **세로 가운데 정렬**로 가파른 대각선 완화, (4) 열 간격/행 간격을 넉넉히(col_gap 66→150, row_h 46→60), (5) **역방향(순환) 간선은 아래로 우회**하되 제어점 x를 두 노드 사이로 유지해 좌우 삐짐 방지, self-loop는 폭 확장으로 처리. bow/loop가 뷰박스 밖으로 잘리지 않도록 높이·폭을 그린 뒤 확장. 좌표 경계·포트 분산은 프로그램으로 검증(모든 좌표 in-bounds, 간선 시작점 전부 distinct). `defaultdict` import 누락 버그 발견·수정. 테스트 1개 추가(총 217 PASS). 재빌드·배포 후 라이브 실측: scope=all에서 viewBox 870×604·간선 10개 시작점 10/10 distinct·전 좌표 in-bounds 확인. 검증용 256Mi 임시 상향 후 128Mi 원복 |
| 2026-07-24 | 대시보드를 메뉴바 형태로 개편 + 메인 화면을 트래픽 흐름으로 변경 | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py | 사용자가 "기본 페이지를 메뉴바 형태로, 메인 화면을 트래픽 흐름으로" 요청. (1) 기존 텍스트 링크 `<nav>`를 상단 **메뉴바**(`.topbar`: 브랜드 + 탭, sticky 고정, 활성 탭 강조)로 개편. (2) 라우팅 변경: **`/`(메인)=트래픽 흐름(Hubble)**, `/policies`=정책 현황(기존 `/`에서 이동), `/flows`는 하위호환 별칭으로 유지(동일 내용). `_flows_url`을 `/flows?`→`/?`(루트 기준)로 바꿔 필터/토글/포커스 링크가 메인에 머물게 함. 흐름 필터 링크는 프리픽스 포함 루트 기준. 테스트 업데이트(정책 테스트는 `/policies`로 이동, 메뉴바/메인=흐름/별칭 테스트 추가, 총 219 PASS). 재빌드·배포 후 라이브 실측: `/`·`/policies`·`/flows` 모두 라우트 도달(무인증 401), 인증 후 `/`=흐름(Hubble)·`/policies`=정책 표 확인, 메뉴바 활성 탭이 페이지별로 정확(프리픽스 포함). 검증용 256Mi 임시 상향 후 128Mi 원복 |
| 2026-07-24 | 대시보드 기본 페이지 디자인을 seoul(k8s-cluster-tester) 대시보드와 통일 | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py | 사용자가 "기본 페이지 디자인을 /ai/seoul에서 쓰는 것처럼 바꿔달라"고 함. `/ai/seoul/apps/k8s-cluster-monitor/frontend/static/css/style.css`의 디자인 시스템을 그대로 채택: (1) CSS 변수 기반 라이트/다크 테마(--bg/--fg/--muted/--border/--card-bg/--critical/--warning/--info 등, seoul 팔레트 동일), (2) 상단바를 pill형에서 seoul식 **border-bottom + 제목(홈 링크)+`.tab`/`.tab.active` 탭**으로 교체, (3) 본문을 `<main>`(padding 20px·max-width 1100px·가운데 정렬)으로 감싸고 페이지 제목은 `h2.page-title`, (4) 표를 seoul `findings-table` 스타일(13px·border var(--border))로, 배지는 pill(999px), code/help/diagram도 변수화. 정책 페이지 heading을 브랜드 중복 피해 "정책 현황"으로. 테스트 갱신(메뉴바 마커 topbar/home-link/tabs/tab.active 기준, 총 219 PASS). 재빌드·배포 후 라이브 실측: `/`·`/policies` 모두 200·seoul 디자인 마커 전부 존재, scope=all/flows 별칭 포함 7연속 요청 전부 200. **기동 직후 첫 요청 1건이 500(과도기 레이스, 이후 동일 요청 모두 200 — 코드 결함 아님으로 판단)**. 검증용 256Mi 임시 상향 후 128Mi 원복 |
| 2026-07-24 | 상단 브랜드 제목 'TrafficPolicy 대시보드' 색상을 파란색(강조색)으로 | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py | 사용자 요청. `.home-link` 색을 `inherit`→`var(--info)`(라이트 #2563eb·다크 #60a5fa, 링크/강조와 동일 파랑)로 변경. 테스트 1개 추가(총 220 PASS). 재빌드·배포 후 라이브 실측: `/policies` 서빙 CSS에서 `.home-link { color: var(--info) }` 확인(경량 경로라 메모리 상향 불필요) |
| 2026-07-24 | 상단 브랜드 제목 'TrafficPolicy 대시보드'를 더 굵게(font-weight 600→800) | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py | 사용자 요청("문자를 두껍게"). `.home-link` font-weight 600→800(extra-bold). 테스트에 굵기 단언 추가(총 220 PASS). 재빌드·배포 후 라이브 실측: 서빙 CSS에서 `.home-link { … font-weight: 800 }` 확인 |
| 2026-07-24 | 배포를 ArgoCD(GitOps)로 전환 — 전체 스택 + 버전 이미지 태그 | operator/deploy/k8s/(신규 GitOps 소스), operator/deploy/argocd/traffic-ops.yaml(신규 Application), README.md | 사용자 요청("배포를 argocd로"). 클러스터에 이미 ArgoCD 설치돼 seoul(k8s-cluster-monitor)/seongnam이 GitOps로 배포 중임을 확인 → 동일 관례 채택. **결정(사용자)**: 관리 범위=전체 스택(CRD+오퍼레이터+대시보드), 이미지=버전 태그로 전환. 매니페스트를 단일 소스 디렉터리 `operator/deploy/k8s/`로 통합(00-crd/10-operator/20-dashboard/21-secret.template) — CRD는 architect 산출물 `operator/crds/trafficpolicy.yaml`이 원본이라 배포용 사본을 두고 "동기화 필수" 주석 명시. 이미지 `:latest`→`:v0.1.0`(dashboard/operator 둘 다 빌드·푸시). Application은 `argocd` ns, project default, source=samsung.git/main path operator/deploy/k8s, automated(prune+selfHeal)+CreateNamespace. **selfHeal이라 앞으로 수동 kubectl 변경(예: 메모리 임시 상향)은 git으로 되돌려짐 — 변경은 git 태그 올려서만**. 대시보드 auth Secret은 여전히 비커밋(배포자 생성, ArgoCD 미추적). org 리포는 public이라 ArgoCD가 인증 없이 clone(seoul/seongnam 동일). |
| 2026-07-24 | `/flows` 화살표를 균일 두께 + '전기 흐르듯' 애니메이션(연결 잦을수록 빠름) + 실제 건수 숫자 표기로 변경(`/loop` 동적모드, **첫 ArgoCD 배포**) | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py, operator/deploy/k8s/20-dashboard.yaml | 사용자가 "두께로 표현 말고 동일 두께에 전기 흐르듯, 건수 잦을수록 빠르게 + 실제 건수 표기"를 요청. 기존 `stroke-width ∝ count`를 제거하고 **균일 두께(2.6px)**로. CSS `@keyframes flow-dash`(stroke-dashoffset 이동)+`.flow-edge`(dasharray 6 8) 도입, 각 선의 `animation-duration`을 상대 빈도(min~max)에 반비례로 인라인 지정(최다=0.5s 빠름, 최소=3.0s 느림) → 파선이 목적지 쪽으로 흐르는 전기 효과. `prefers-reduced-motion`이면 애니메이션 끄고 실선(숫자로 정보 유지). 화살표 가운데에 실제 연결 수를 배경 halo 텍스트로 표기(3차 베지어 t=0.5 중점). 설명 패널/캡션도 갱신. 테스트 3개 추가(총 223 PASS). **배포는 ArgoCD로 전환 후 첫 릴리스**: dashboard 이미지 v0.1.0→v0.1.1 빌드·푸시하고 20-dashboard.yaml 태그만 올려 커밋·push(=`kubectl rollout` 안 씀). ArgoCD 자동 동기화 검증 예정 |
| 2026-07-24 | 대시보드 OOM 방지 성능조정(hubble 조회 single-flight + TTL 캐시, 메모리 상한 상향) | operator/k8s_traffic_operator/dashboard/hubble_flows.py, operator/tests/test_hubble_flows.py, operator/deploy/k8s/20-dashboard.yaml | 사용자가 "OOM 안 나게 성능조정"을 요청(화살표 애니메이션은 v0.1.1에서 이미 완료). 근본원인: `/flows`가 매 요청마다 hubble CLI(별도 Go 프로세스, 큰 RSS)를 띄우는데 10초 자동새로고침+동시 뷰어로 프로세스가 겹쳐 128Mi 초과. 해결: hubble_flows에 `_fetch_flows_cached`(threading.Lock으로 single-flight + `HUBBLE_CACHE_TTL_S`(기본 6초) TTL 캐시) 추가 — 동시 서브프로세스를 1개로 제한하고 TTL 내 재요청은 캐시 재사용(실패는 미캐시). fetch_summary가 이를 사용. 메모리 상한 128Mi→256Mi, requests 64→96Mi(코드 최적화와 별개 안전 여유). 캐시 격리용 autouse 픽스처 + 캐시 동작 테스트 3개 추가(총 226 PASS). 배포: dashboard v0.1.1→v0.1.2 빌드·푸시, 20-dashboard.yaml 태그·메모리 수정 커밋·push → ArgoCD 동기화. 상한 256Mi라 이제 검증용 in-pod 요청도 OOM 없이 수행 |
| 2026-07-24 | 자동 새로고침 컨트롤을 상단바로 이동 + 셀 형태 세그먼트 버튼(표)으로 재구현(`/loop` 동적모드) | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py, operator/deploy/k8s/20-dashboard.yaml | 사용자가 "자동 새로고침을 상단바로 옮기고 상태를 표로 다시 구현"을 요청. **사용자 결정(AskUserQuestion)**: 표 형태=세그먼트 버튼(셀), 배포=빌드+배포. (1) `_PAGE_TEMPLATE`에서 `{refresh}`를 `<main>`→상단바 우측 `.topbar-right`(자동 새로고침 + userbox 묶음, margin-left:auto)로 이동. (2) `_refresh_selector_html`을 인라인 텍스트 링크(` · ` 구분)에서 **셀 세그먼트 버튼**으로 재구현: `.seg-cells`(테두리+radius+overflow hidden) 안에 옵션별 `.seg` 링크, 현재 선택은 `.seg.active`(파란 배경+흰 글자)+`aria-current`. 무JS 유지(링크 이동+meta refresh) — 드롭다운(select) 대신 링크식이라 기존 무JS 원칙과 일치. `.topbar-right`/`.refresh-seg`/`.seg` CSS 추가. 정책/흐름 두 페이지 공통(각 페이지의 option_url만 다름), 로그인/설정 페이지는 원래대로 새로고침/세그먼트 없음. 테스트: refresh-ctl→refresh-seg 갱신 + 상단바 위치/active/aria 검증 1개 추가(총 **261 PASS**). 배포: dashboard v0.1.4→v0.1.5 빌드·푸시, 태그 커밋·push → ArgoCD 동기화 |
| 2026-07-24 | 대시보드 자동 새로고침을 기본 1분으로 + 10초/30초/1분/10분/끄기 선택 UI 추가(`/loop` 동적모드) | operator/k8s_traffic_operator/dashboard/app.py, operator/tests/test_dashboard.py, operator/deploy/k8s/20-dashboard.yaml | 사용자가 "자동 새로고침을 1분 기준으로 + 10초/30초/1분/10분/새로고침 안함 선택 가능하게"를 요청. 기존 `<meta http-equiv=refresh content=10>` 하드코딩(10초 고정)을 제거하고 쿼리 `refresh`(10/30/60/600/off, 기본 60=1분)로 제어. `_refresh_var` contextvar + `_normalize_refresh`(허용 밖이면 1분)·`_refresh_meta_tag`(off면 meta 없음)·`_refresh_selector_html`(옵션 링크·active 표시) 추가. `_page`에 `{refresh_meta}`·`{refresh}` 슬롯 신설, 콘텐츠 페이지(흐름/정책) 상단에 선택 컨트롤 노출. **meta refresh는 현재 URL을 그대로 다시 부르므로 필터가 유지되며**, 추가로 `_flows_url`이 모든 흐름 링크에 현재 refresh를 실어 필터 클릭 시에도 선택 유지(정책 페이지는 `/policies?refresh=`). 라우트 3개(`/`,`/flows`,`/policies`)에 `refresh` 파라미터+contextvar 세팅. 로그인/설정(`_AUTH_PAGE_TEMPLATE`)은 애초에 자동 새로고침 없음(폼 입력 보호) — 그대로. 테스트 7개 추가(총 **260 PASS**), refresh 파라미터로 `_flows_url` 출력이 길어져 기존 URL 정확일치 단언 3개를 접두 검사로 완화. 배포: dashboard v0.1.3→v0.1.4 빌드·푸시, 태그 커밋·push → ArgoCD 동기화 |
| 2026-07-24 | 브랜드 제목에서 '대시보드' 제거 + HTTP Basic → 앱 내 폼 로그인/설정(비번 변경) 전환(`/loop` 동적모드) | operator/k8s_traffic_operator/dashboard/{auth.py(신규),app.py}, operator/tests/{test_auth.py(신규),test_dashboard.py}, operator/Dockerfile.dashboard, operator/requirements.txt, operator/deploy/k8s/20-dashboard.yaml | 사용자가 "메인 상단 'TrafficPolicy 대시보드'에서 '대시보드' 제거 + 로그인 admin/password로 구성, 수정 가능하게"를 요청. **사용자 결정(AskUserQuestion)**: env 하드코딩/Secret 주입이 아니라 **앱 내 로그인 페이지 + 비번 변경 UI**(read-only 설계에서 벗어나는 큰 작업임을 알고 선택). (1) 브랜드 `.home-link` 텍스트 `TrafficPolicy 대시보드`→`TrafficPolicy`. (2) 신규 `auth.CredentialStore`: **파일 기반**(볼륨) 저장 — 클러스터 API엔 여전히 무쓰기, 자기 볼륨에만 씀. 기본 자격증명 **admin/password**, 비번은 PBKDF2-HMAC-SHA256(salt) 해시(평문 저장 안 함), env `DASHBOARD_USERNAME/PASSWORD`는 파일 최초 생성 시 **부트스트랩 시드**(optional)로만. (3) 인증을 Basic→**HMAC 서명 세션 쿠키**(폼 로그인)로 전환, `/login`·`/logout`·`/settings` 라우트 추가, 상단바에 사용자명+설정+로그아웃(contextvar로 렌더). 스크립트용 **Basic도 저장값과 일치 시 허용**(`/api`는 리다이렉트 대신 401). 비번/아이디 변경 시 **세션 키 회전→타 기기 강제 로그아웃**, 오픈 리다이렉트 방지(`_safe_next`). 폼 파싱 위해 `python-multipart` 추가(Dockerfile/requirements/venv). (4) 매니페스트: 자격증명 **PVC(64Mi, RWO) + `/data` 마운트 + fsGroup 1001** 추가(비번 변경이 재기동에도 유지), Secret env `optional: true`(없어도 기동), 이미지 태그 v0.1.2→v0.1.3, 예전 503 fail-closed 문구 제거. **주의: 기존 무인증 접근이 이제 로그인 폼으로 302(HTML)/401(API)** — 옛 Basic 테스트를 세션/폼 기준으로 재작성. 테스트 총 226→**253 PASS**(auth 유닛 15 + dashboard 로그인/설정 12). **코드/테스트/매니페스트만 완료 — v0.1.3 이미지 빌드·푸시 및 커밋(=ArgoCD 자동배포)은 사용자 확인 대기** |
