# k8s-traffic-operator 런타임 의존성
kopf>=1.37,<2.0            # Kubernetes Operator 프레임워크 (핸들러/타이머)
kubernetes>=29.0.0        # actuator: Deployment/HTTPRoute 리소스 패치
requests>=2.31.0          # metrics: Prometheus HTTP API 조회

# 대시보드 (선택 — operator.dashboard, 별도 프로세스로 배포)
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9    # 대시보드 로그인/설정 폼 파싱

# 개발/테스트
pytest>=8.0.0
httpx>=0.27.0               # FastAPI TestClient 의존성 (테스트 전용)
